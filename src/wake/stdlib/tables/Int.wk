every Int is:

	Int -- squared() { return 0; }
	Int -- abs() { return 0; }

	Int -- orMaxOf(Int) { return 0; }
	Int -- orMaxOf(Int[]) { return 0; }

	Int -- orMinOf(Int) { return 0; }
	Int -- orMinOf(Int[]) { return 0; }

	Int -- numberOfDigits() { return 0; }
